# Backend Configuration
# URL do backend (pode ser localhost ou IP da VPS)
VITE_BACKEND_URL=http://localhost:3001

# Para produção na VPS, troque para:
# VITE_BACKEND_URL=http://SEU_IP_VPS:3001
# Ou domínio:
# VITE_BACKEND_URL=https://seu-dominio.com

# Configurações de CORS (para o backend)
# Liste os domínios que podem acessar o backend
CORS_ORIGINS=*
# Ou específico:
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://seu-frontend.com